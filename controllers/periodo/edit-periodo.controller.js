angular.module('dataCoffee').controller('editPeriodo', editPeriodo);

editPeriodo.$inject = ['$scope', '$routeParams', '$rootScope', '$route', '$location'];

function editPeriodo($scope, $routeParams, $routeScope, $route, $location) {
    
}