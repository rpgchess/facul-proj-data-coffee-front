angular.module('dataCoffee').controller('newAtividade', newAtividade);

newAtividade.$inject = ['$scope', '$routeParams', '$rootScope', '$route', '$location'];

function newAtividade($scope, $routeParams, $routeScope, $route, $location) {
    
}