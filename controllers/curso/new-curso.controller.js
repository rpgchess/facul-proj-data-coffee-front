angular.module('dataCoffee').controller('newCurso', newCurso);

newCurso.$inject = ['$scope', '$routeParams', '$rootScope', '$route', '$location'];

function newCurso($scope, $routeParams, $routeScope, $route, $location) {
    
}